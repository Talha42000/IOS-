import SwiftUI
import Foundation

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello, world!")
        }
    }
}

//Loops tyoe If loops : For Loops : While Loop
// Loops is the collection of number which runs the code unlimit number of times

//For loop

//let numbers = [1, 2, 3, 4, 5, 6]

//for thing in numbers {
//    let result = thing * 2
  //  print (result)
//}

//while loop has a notion to do something while this condition is met

var x = 0

while x <= 3 {
    print ("text")
    x += 1
}

