import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.orange)
        }
    }
}
// Optionals

var x: Int? = 3
var y: Int? = 12

var price: Double? = nil

//print (price)
//price = 1.23
//print (price)

//type = price

